name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    runs-on: ubuntu-24.04
    outputs:
      created: ${{ steps.release.outputs.release_created }}
      tag: ${{ steps.release.outputs.tag_name }}
      version: ${{ steps.release.outputs.version }}
    steps:
      - id: release
        uses: googleapis/release-please-action@v5
        with:
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

  build:
    if: needs.release.outputs.created == 'true'
    needs: release
    name: Build ${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: darwin-arm64
            runner: macos-15
            wails_platform: darwin/arm64
          - platform: windows-amd64
            runner: windows-2025
            wails_platform: windows/amd64
          - platform: linux-amd64
            runner: ubuntu-24.04
            wails_platform: linux/amd64
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    env:
      VERSION: ${{ needs.release.outputs.version }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ needs.release.outputs.tag }}
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Configure Ubuntu package mirror
        if: matrix.platform == 'linux-amd64'
        shell: bash
        run: bash scripts/configure-ubuntu-mirror.sh
      - name: Install Linux build dependencies
        if: matrix.platform == 'linux-amd64'
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends \
            build-essential file libgtk-3-dev libwebkit2gtk-4.1-dev
      - name: Install frontend dependencies
        shell: bash
        run: |
          npm install --global npm@12.0.2
          npm ci --prefix frontend
      - name: Install Wails
        shell: bash
        run: go install github.com/wailsapp/wails/v2/cmd/wails@v2.14.0
      - name: Build Client
        shell: bash
        run: |
          tags=()
          if [[ "${{ matrix.platform }}" == "linux-amd64" ]]; then
            tags=(-tags webkit2_41)
          fi
          "$(go env GOPATH)/bin/wails" build \
            -clean -trimpath -m -nosyncgomod \
            -platform "${{ matrix.wails_platform }}" \
            "${tags[@]}" \
            -ldflags "-s -w -X main.desktopVersion=$VERSION"
      - name: Package macOS Client
        if: matrix.platform == 'darwin-arm64'
        shell: bash
        run: |
          mkdir -p build/release
          tar -czf "build/release/cineko-client-v${VERSION}-darwin-arm64.tar.gz" \
            -C build/bin Cineko.app
      - name: Package Windows Client
        if: matrix.platform == 'windows-amd64'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path build/release | Out-Null
          Compress-Archive -LiteralPath build/bin/Cineko.exe `
            -DestinationPath "build/release/cineko-client-v${env:VERSION}-windows-amd64.zip"
      - name: Package Linux Client
        if: matrix.platform == 'linux-amd64'
        shell: bash
        run: |
          mkdir -p build/release
          tar -czf "build/release/cineko-client-v${VERSION}-linux-amd64.tar.gz" \
            -C build/bin Cineko
      - uses: actions/upload-artifact@v7
        with:
          name: client-${{ matrix.platform }}
          path: build/release/cineko-client-v${{ needs.release.outputs.version }}-*
          if-no-files-found: error
          compression-level: 0

  publish:
    if: needs.release.outputs.created == 'true'
    needs:
      - release
      - build
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    env:
      CINEKO_CENTRAL_URL: ${{ secrets.CINEKO_CENTRAL_URL }}
      CINEKO_RELEASE_PUBLISH_TOKEN: ${{ secrets.CINEKO_RELEASE_PUBLISH_TOKEN }}
      CINEKO_PROBE_BOOTSTRAP_PUBLIC_KEYS_JSON: ${{ secrets.CINEKO_PROBE_BOOTSTRAP_PUBLIC_KEYS_JSON }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ needs.release.outputs.tag }}
      - uses: actions/download-artifact@v8
        with:
          pattern: client-*
          path: release-assets
          merge-multiple: true
      - name: Create checksums
        working-directory: release-assets
        run: sha256sum cineko-client-* > SHA256SUMS
      - name: Attach portable Clients
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          gh release upload
          "${{ needs.release.outputs.tag }}"
          release-assets/cineko-client-*
          release-assets/SHA256SUMS
          --repo "${{ github.repository }}"
      - name: Register the stable Client set
        env:
          CINEKO_CLIENT_RELEASE_BASE: https://github.com/${{ github.repository }}/releases/download/${{ needs.release.outputs.tag }}
        run: |
          published_at="$(date --utc --date="@$(git show -s --format=%ct HEAD)" +%Y-%m-%dT%H:%M:%SZ)"
          scripts/register-client-release.sh \
            "${{ needs.release.outputs.version }}" \
            "$published_at" \
            release-assets \
            release-compatibility.json
